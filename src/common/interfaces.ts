export interface CommonPost {
  id: number;
  title: string;
  text: string;
  isActive: boolean;
}

export interface CommonUser {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
}
